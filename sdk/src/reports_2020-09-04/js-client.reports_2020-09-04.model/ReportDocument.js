/*
 * Selling Partner API for Reports
 * Effective **June 27, 2024**, the Selling Partner API for Reports v2020-09-04 will no longer be available and all calls to it will fail. Integrations that rely on the Reports API must migrate to [Reports v2021-06-30](https://developer-docs.amazon.com/sp-api/docs/reports-api-v2021-06-30-reference) to avoid service disruption.
 *
 * OpenAPI spec version: 2020-09-04
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.29
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient.js';
import {ReportDocumentEncryptionDetails} from './ReportDocumentEncryptionDetails.js';

/**
 * The ReportDocument model module.
 * @module reports_2020-09-04/js-client.reports_2020-09-04.model/ReportDocument
 * @version 2020-09-04
 */
export class ReportDocument {
  /**
   * Constructs a new <code>ReportDocument</code>.
   * @alias module:reports_2020-09-04/js-client.reports_2020-09-04.model/ReportDocument
   * @class
   * @param reportDocumentId {String} The identifier for the report document. This identifier is unique only in combination with a seller ID.
   * @param url {String} A presigned URL for the report document. If `compressionAlgorithm` is not returned, you can download the report directly from this URL. This URL expires after 5 minutes.
   * @param encryptionDetails {module:reports_2020-09-04/js-client.reports_2020-09-04.model/ReportDocumentEncryptionDetails} 
   */
  constructor(reportDocumentId, url, encryptionDetails) {
    this.reportDocumentId = reportDocumentId;
    this.url = url;
    this.encryptionDetails = encryptionDetails;
  }

  /**
   * Constructs a <code>ReportDocument</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:reports_2020-09-04/js-client.reports_2020-09-04.model/ReportDocument} obj Optional instance to populate.
   * @return {module:reports_2020-09-04/js-client.reports_2020-09-04.model/ReportDocument} The populated <code>ReportDocument</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      switch(typeof data) {
        case 'string':
          obj = String(data);
          break;
        case 'number':
          obj = Number(data);
          break;
        case 'boolean':
          obj = Boolean(data);
          break;
      }
      obj = obj || new ReportDocument();
      if (data.hasOwnProperty('reportDocumentId'))
        obj.reportDocumentId = ApiClient.convertToType(data['reportDocumentId'], 'String');
      if (data.hasOwnProperty('url'))
        obj.url = ApiClient.convertToType(data['url'], 'String');
      if (data.hasOwnProperty('encryptionDetails'))
        obj.encryptionDetails = ReportDocumentEncryptionDetails.constructFromObject(data['encryptionDetails']);
      if (data.hasOwnProperty('compressionAlgorithm'))
        obj.compressionAlgorithm = ApiClient.convertToType(data['compressionAlgorithm'], 'String');
    }
    return obj;
  }
}

/**
 * The identifier for the report document. This identifier is unique only in combination with a seller ID.
 * @member {String} reportDocumentId
 */
ReportDocument.prototype.reportDocumentId = undefined;

/**
 * A presigned URL for the report document. If `compressionAlgorithm` is not returned, you can download the report directly from this URL. This URL expires after 5 minutes.
 * @member {String} url
 */
ReportDocument.prototype.url = undefined;

/**
 * @member {module:reports_2020-09-04/js-client.reports_2020-09-04.model/ReportDocumentEncryptionDetails} encryptionDetails
 */
ReportDocument.prototype.encryptionDetails = undefined;

/**
 * Allowed values for the <code>compressionAlgorithm</code> property.
 * @enum {String}
 * @readonly
 */
ReportDocument.CompressionAlgorithmEnum = {
  /**
   * value: "GZIP"
   * @const
   */
  GZIP: "GZIP"
};

/**
 * If the report document contents have been compressed, the compression algorithm used is returned in this property and you must decompress the report when you download. Otherwise, you can download the report directly. Refer to [Step 2. Download and decrypt the report](doc:reports-api-v2020-09-04-use-case-guide#step-2-download-and-decrypt-the-report) in the use case guide, where sample code is provided.
 * @member {module:reports_2020-09-04/js-client.reports_2020-09-04.model/ReportDocument.CompressionAlgorithmEnum} compressionAlgorithm
 */
ReportDocument.prototype.compressionAlgorithm = undefined;


