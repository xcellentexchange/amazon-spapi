/*
 * Selling Partner API for Finances
 * The Selling Partner API for Finances provides financial information that is relevant to a seller's business. You can obtain financial events for a given order, financial event group, or date range without having to wait until a statement period closes. You can also obtain financial event groups for a given date range. 
 *
 * OpenAPI spec version: v0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.29
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient.js';
import {Currency} from './Currency.js';

/**
 * The RemovalShipmentItem model module.
 * @module financesV0/js-client.financesV0.model/RemovalShipmentItem
 * @version v0
 */
export class RemovalShipmentItem {
  /**
   * Constructs a new <code>RemovalShipmentItem</code>.
   * Item-level information for a removal shipment.
   * @alias module:financesV0/js-client.financesV0.model/RemovalShipmentItem
   * @class
   */
  constructor() {
  }

  /**
   * Constructs a <code>RemovalShipmentItem</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:financesV0/js-client.financesV0.model/RemovalShipmentItem} obj Optional instance to populate.
   * @return {module:financesV0/js-client.financesV0.model/RemovalShipmentItem} The populated <code>RemovalShipmentItem</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      switch(typeof data) {
        case 'string':
          obj = String(data);
          break;
        case 'number':
          obj = Number(data);
          break;
        case 'boolean':
          obj = Boolean(data);
          break;
      }
      obj = obj || new RemovalShipmentItem();
      if (data.hasOwnProperty('RemovalShipmentItemId'))
        obj.removalShipmentItemId = ApiClient.convertToType(data['RemovalShipmentItemId'], 'String');
      if (data.hasOwnProperty('TaxCollectionModel'))
        obj.taxCollectionModel = ApiClient.convertToType(data['TaxCollectionModel'], 'String');
      if (data.hasOwnProperty('FulfillmentNetworkSKU'))
        obj.fulfillmentNetworkSKU = ApiClient.convertToType(data['FulfillmentNetworkSKU'], 'String');
      if (data.hasOwnProperty('Quantity'))
        obj.quantity = ApiClient.convertToType(data['Quantity'], 'Number');
      if (data.hasOwnProperty('Revenue'))
        obj.revenue = Currency.constructFromObject(data['Revenue']);
      if (data.hasOwnProperty('FeeAmount'))
        obj.feeAmount = Currency.constructFromObject(data['FeeAmount']);
      if (data.hasOwnProperty('TaxAmount'))
        obj.taxAmount = Currency.constructFromObject(data['TaxAmount']);
      if (data.hasOwnProperty('TaxWithheld'))
        obj.taxWithheld = Currency.constructFromObject(data['TaxWithheld']);
    }
    return obj;
  }
}

/**
 * An identifier for an item in a removal shipment.
 * @member {String} removalShipmentItemId
 */
RemovalShipmentItem.prototype.removalShipmentItemId = undefined;

/**
 * The tax collection model that is applied to the item.  Possible values:  * `MarketplaceFacilitator`: Tax is withheld and remitted to the taxing authority by Amazon on behalf of the seller. * `Standard`: Tax is paid to the seller and not remitted to the taxing authority by Amazon.
 * @member {String} taxCollectionModel
 */
RemovalShipmentItem.prototype.taxCollectionModel = undefined;

/**
 * The Amazon fulfillment network SKU for the item.
 * @member {String} fulfillmentNetworkSKU
 */
RemovalShipmentItem.prototype.fulfillmentNetworkSKU = undefined;

/**
 * The quantity of the item.
 * @member {Number} quantity
 */
RemovalShipmentItem.prototype.quantity = undefined;

/**
 * The total amount paid to the seller for the removed item.
 * @member {module:financesV0/js-client.financesV0.model/Currency} revenue
 */
RemovalShipmentItem.prototype.revenue = undefined;

/**
 * The fee that Amazon charged to the seller for the removal of the item. The amount is a negative number.
 * @member {module:financesV0/js-client.financesV0.model/Currency} feeAmount
 */
RemovalShipmentItem.prototype.feeAmount = undefined;

/**
 * Tax collected on the revenue.
 * @member {module:financesV0/js-client.financesV0.model/Currency} taxAmount
 */
RemovalShipmentItem.prototype.taxAmount = undefined;

/**
 * The tax withheld and remitted to the taxing authority by Amazon on behalf of the seller. If `TaxCollectionModel` = `MarketplaceFacilitator`, then `TaxWithheld` = `TaxAmount` (except when the `TaxWithheld` amount is a negative number). Otherwise, `TaxWithheld` = `0`.
 * @member {module:financesV0/js-client.financesV0.model/Currency} taxWithheld
 */
RemovalShipmentItem.prototype.taxWithheld = undefined;


