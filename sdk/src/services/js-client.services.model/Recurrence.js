/*
 * Selling Partner API for Services
 * With the Services API, you can build applications that help service providers get and modify their service orders and manage their resources.
 *
 * OpenAPI spec version: v1
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.29
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient.js';
import {DayOfWeek} from './DayOfWeek.js';

/**
 * The Recurrence model module.
 * @module services/js-client.services.model/Recurrence
 * @version v1
 */
export class Recurrence {
  /**
   * Constructs a new <code>Recurrence</code>.
   * Repeated occurrence of an event in a time range.
   * @alias module:services/js-client.services.model/Recurrence
   * @class
   * @param endTime {Date} End time of the recurrence.
   */
  constructor(endTime) {
    this.endTime = endTime;
  }

  /**
   * Constructs a <code>Recurrence</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:services/js-client.services.model/Recurrence} obj Optional instance to populate.
   * @return {module:services/js-client.services.model/Recurrence} The populated <code>Recurrence</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      switch(typeof data) {
        case 'string':
          obj = String(data);
          break;
        case 'number':
          obj = Number(data);
          break;
        case 'boolean':
          obj = Boolean(data);
          break;
      }
      obj = obj || new Recurrence();
      if (data.hasOwnProperty('endTime'))
        obj.endTime = ApiClient.convertToType(data['endTime'], 'Date');
      if (data.hasOwnProperty('daysOfWeek'))
        obj.daysOfWeek = ApiClient.convertToType(data['daysOfWeek'], [DayOfWeek]);
      if (data.hasOwnProperty('daysOfMonth'))
        obj.daysOfMonth = ApiClient.convertToType(data['daysOfMonth'], ['Number']);
    }
    return obj;
  }
}

/**
 * End time of the recurrence.
 * @member {Date} endTime
 */
Recurrence.prototype.endTime = undefined;

/**
 * Days of the week when recurrence is valid. If the schedule is valid every Monday, input will only contain `MONDAY` in the list.
 * @member {Array.<module:services/js-client.services.model/DayOfWeek>} daysOfWeek
 */
Recurrence.prototype.daysOfWeek = undefined;

/**
 * Days of the month when recurrence is valid.
 * @member {Array.<Number>} daysOfMonth
 */
Recurrence.prototype.daysOfMonth = undefined;


