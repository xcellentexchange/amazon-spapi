/*
 * Selling Partner API for Feeds
 * Effective **June 27, 2024**, the Selling Partner API for Feeds v2020-09-04 will no longer be available and all calls to it will fail. Integrations that rely on the Feeds API must migrate to [Feeds v2021-06-30](https://developer-docs.amazon.com/sp-api/docs/feeds-api-v2021-06-30-reference) to avoid service disruption.
 *
 * OpenAPI spec version: 2020-09-04
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.29
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient.js';
import {FeedDocumentEncryptionDetails} from './FeedDocumentEncryptionDetails.js';

/**
 * The FeedDocument model module.
 * @module feeds_2020-09-04/js-client.feeds_2020-09-04.model/FeedDocument
 * @version 2020-09-04
 */
export class FeedDocument {
  /**
   * Constructs a new <code>FeedDocument</code>.
   * @alias module:feeds_2020-09-04/js-client.feeds_2020-09-04.model/FeedDocument
   * @class
   * @param feedDocumentId {String} The identifier for the feed document. This identifier is unique only in combination with a seller ID.
   * @param url {String} A presigned URL for the feed document. If `compressionAlgorithm` is not returned, you can download the feed directly from this URL. This URL expires after 5 minutes.
   * @param encryptionDetails {module:feeds_2020-09-04/js-client.feeds_2020-09-04.model/FeedDocumentEncryptionDetails} 
   */
  constructor(feedDocumentId, url, encryptionDetails) {
    this.feedDocumentId = feedDocumentId;
    this.url = url;
    this.encryptionDetails = encryptionDetails;
  }

  /**
   * Constructs a <code>FeedDocument</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:feeds_2020-09-04/js-client.feeds_2020-09-04.model/FeedDocument} obj Optional instance to populate.
   * @return {module:feeds_2020-09-04/js-client.feeds_2020-09-04.model/FeedDocument} The populated <code>FeedDocument</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      switch(typeof data) {
        case 'string':
          obj = String(data);
          break;
        case 'number':
          obj = Number(data);
          break;
        case 'boolean':
          obj = Boolean(data);
          break;
      }
      obj = obj || new FeedDocument();
      if (data.hasOwnProperty('feedDocumentId'))
        obj.feedDocumentId = ApiClient.convertToType(data['feedDocumentId'], 'String');
      if (data.hasOwnProperty('url'))
        obj.url = ApiClient.convertToType(data['url'], 'String');
      if (data.hasOwnProperty('encryptionDetails'))
        obj.encryptionDetails = FeedDocumentEncryptionDetails.constructFromObject(data['encryptionDetails']);
      if (data.hasOwnProperty('compressionAlgorithm'))
        obj.compressionAlgorithm = ApiClient.convertToType(data['compressionAlgorithm'], 'String');
    }
    return obj;
  }
}

/**
 * The identifier for the feed document. This identifier is unique only in combination with a seller ID.
 * @member {String} feedDocumentId
 */
FeedDocument.prototype.feedDocumentId = undefined;

/**
 * A presigned URL for the feed document. If `compressionAlgorithm` is not returned, you can download the feed directly from this URL. This URL expires after 5 minutes.
 * @member {String} url
 */
FeedDocument.prototype.url = undefined;

/**
 * @member {module:feeds_2020-09-04/js-client.feeds_2020-09-04.model/FeedDocumentEncryptionDetails} encryptionDetails
 */
FeedDocument.prototype.encryptionDetails = undefined;

/**
 * Allowed values for the <code>compressionAlgorithm</code> property.
 * @enum {String}
 * @readonly
 */
FeedDocument.CompressionAlgorithmEnum = {
  /**
   * value: "GZIP"
   * @const
   */
  GZIP: "GZIP"
};

/**
 * If the feed document contents have been compressed, the compression algorithm used is returned in this property and you must decompress the feed when you download. Otherwise, you can download the feed directly. Refer to [Step 6. Download and decrypt the feed processing report](doc:feeds-api-v2020-09-04-use-case-guide#step-6-download-and-decrypt-the-feed-processing-report) in the use case guide, where sample code is provided.
 * @member {module:feeds_2020-09-04/js-client.feeds_2020-09-04.model/FeedDocument.CompressionAlgorithmEnum} compressionAlgorithm
 */
FeedDocument.prototype.compressionAlgorithm = undefined;


