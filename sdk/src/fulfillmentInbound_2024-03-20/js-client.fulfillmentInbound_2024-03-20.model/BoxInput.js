/*
 * The Selling Partner API for FBA inbound operations.
 * The Selling Partner API for Fulfillment By Amazon (FBA) Inbound. The FBA Inbound API enables building inbound workflows to create, manage, and send shipments into Amazon's fulfillment network. The API has interoperability with the Send-to-Amazon user interface.
 *
 * OpenAPI spec version: 2024-03-20
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.29
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient.js';
import {BoxContentInformationSource} from './BoxContentInformationSource.js';
import {Dimensions} from './Dimensions.js';
import {ItemInput} from './ItemInput.js';
import {Weight} from './Weight.js';

/**
 * The BoxInput model module.
 * @module fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/BoxInput
 * @version 2024-03-20
 */
export class BoxInput {
  /**
   * Constructs a new <code>BoxInput</code>.
   * Input information for a given box.
   * @alias module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/BoxInput
   * @class
   * @param contentInformationSource {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/BoxContentInformationSource} 
   * @param dimensions {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/Dimensions} 
   * @param quantity {Number} The number of containers where all other properties like weight or dimensions are identical.
   * @param weight {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/Weight} 
   */
  constructor(contentInformationSource, dimensions, quantity, weight) {
    this.contentInformationSource = contentInformationSource;
    this.dimensions = dimensions;
    this.quantity = quantity;
    this.weight = weight;
  }

  /**
   * Constructs a <code>BoxInput</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/BoxInput} obj Optional instance to populate.
   * @return {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/BoxInput} The populated <code>BoxInput</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      switch(typeof data) {
        case 'string':
          obj = String(data);
          break;
        case 'number':
          obj = Number(data);
          break;
        case 'boolean':
          obj = Boolean(data);
          break;
      }
      obj = obj || new BoxInput();
      if (data.hasOwnProperty('contentInformationSource'))
        obj.contentInformationSource = BoxContentInformationSource.constructFromObject(data['contentInformationSource']);
      if (data.hasOwnProperty('dimensions'))
        obj.dimensions = Dimensions.constructFromObject(data['dimensions']);
      if (data.hasOwnProperty('items'))
        obj.items = ApiClient.convertToType(data['items'], [ItemInput]);
      if (data.hasOwnProperty('quantity'))
        obj.quantity = ApiClient.convertToType(data['quantity'], 'Number');
      if (data.hasOwnProperty('weight'))
        obj.weight = Weight.constructFromObject(data['weight']);
    }
    return obj;
  }
}

/**
 * @member {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/BoxContentInformationSource} contentInformationSource
 */
BoxInput.prototype.contentInformationSource = undefined;

/**
 * @member {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/Dimensions} dimensions
 */
BoxInput.prototype.dimensions = undefined;

/**
 * The items and their quantity in the box. This must be empty if the box `contentInformationSource` is `BARCODE_2D` or `MANUAL_PROCESS`.
 * @member {Array.<module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/ItemInput>} items
 */
BoxInput.prototype.items = undefined;

/**
 * The number of containers where all other properties like weight or dimensions are identical.
 * @member {Number} quantity
 */
BoxInput.prototype.quantity = undefined;

/**
 * @member {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/Weight} weight
 */
BoxInput.prototype.weight = undefined;


