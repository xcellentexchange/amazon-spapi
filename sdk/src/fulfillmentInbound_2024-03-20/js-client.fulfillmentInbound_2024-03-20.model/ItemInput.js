/*
 * The Selling Partner API for FBA inbound operations.
 * The Selling Partner API for Fulfillment By Amazon (FBA) Inbound. The FBA Inbound API enables building inbound workflows to create, manage, and send shipments into Amazon's fulfillment network. The API has interoperability with the Send-to-Amazon user interface.
 *
 * OpenAPI spec version: 2024-03-20
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.29
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient.js';
import {LabelOwner} from './LabelOwner.js';
import {PrepOwner} from './PrepOwner.js';

/**
 * The ItemInput model module.
 * @module fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/ItemInput
 * @version 2024-03-20
 */
export class ItemInput {
  /**
   * Constructs a new <code>ItemInput</code>.
   * Defines an item's input parameters.
   * @alias module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/ItemInput
   * @class
   * @param labelOwner {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/LabelOwner} 
   * @param msku {String} The merchant SKU, a merchant-supplied identifier of a specific SKU.
   * @param prepOwner {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/PrepOwner} 
   * @param quantity {Number} The number of units of the specified MSKU that will be shipped.
   */
  constructor(labelOwner, msku, prepOwner, quantity) {
    this.labelOwner = labelOwner;
    this.msku = msku;
    this.prepOwner = prepOwner;
    this.quantity = quantity;
  }

  /**
   * Constructs a <code>ItemInput</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/ItemInput} obj Optional instance to populate.
   * @return {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/ItemInput} The populated <code>ItemInput</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      switch(typeof data) {
        case 'string':
          obj = String(data);
          break;
        case 'number':
          obj = Number(data);
          break;
        case 'boolean':
          obj = Boolean(data);
          break;
      }
      obj = obj || new ItemInput();
      if (data.hasOwnProperty('expiration'))
        obj.expiration = ApiClient.convertToType(data['expiration'], 'String');
      if (data.hasOwnProperty('labelOwner'))
        obj.labelOwner = LabelOwner.constructFromObject(data['labelOwner']);
      if (data.hasOwnProperty('manufacturingLotCode'))
        obj.manufacturingLotCode = ApiClient.convertToType(data['manufacturingLotCode'], 'String');
      if (data.hasOwnProperty('msku'))
        obj.msku = ApiClient.convertToType(data['msku'], 'String');
      if (data.hasOwnProperty('prepOwner'))
        obj.prepOwner = PrepOwner.constructFromObject(data['prepOwner']);
      if (data.hasOwnProperty('quantity'))
        obj.quantity = ApiClient.convertToType(data['quantity'], 'Number');
    }
    return obj;
  }
}

/**
 * The expiration date of the MSKU. In [ISO 8601](https://developer-docs.amazon.com/sp-api/docs/iso-8601) datetime format with pattern `YYYY-MM-DD`. Items with the same MSKU but different expiration dates cannot go into the same box.
 * @member {String} expiration
 */
ItemInput.prototype.expiration = undefined;

/**
 * @member {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/LabelOwner} labelOwner
 */
ItemInput.prototype.labelOwner = undefined;

/**
 * The manufacturing lot code.
 * @member {String} manufacturingLotCode
 */
ItemInput.prototype.manufacturingLotCode = undefined;

/**
 * The merchant SKU, a merchant-supplied identifier of a specific SKU.
 * @member {String} msku
 */
ItemInput.prototype.msku = undefined;

/**
 * @member {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/PrepOwner} prepOwner
 */
ItemInput.prototype.prepOwner = undefined;

/**
 * The number of units of the specified MSKU that will be shipped.
 * @member {Number} quantity
 */
ItemInput.prototype.quantity = undefined;


