/*
 * The Selling Partner API for FBA inbound operations.
 * The Selling Partner API for Fulfillment By Amazon (FBA) Inbound. The FBA Inbound API enables building inbound workflows to create, manage, and send shipments into Amazon's fulfillment network. The API has interoperability with the Send-to-Amazon user interface.
 *
 * OpenAPI spec version: 2024-03-20
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.29
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient.js';
import {BoxContentInformationSource} from './BoxContentInformationSource.js';
import {Dimensions} from './Dimensions.js';
import {Item} from './Item.js';
import {Region} from './Region.js';
import {Weight} from './Weight.js';

/**
 * The Box model module.
 * @module fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/Box
 * @version 2024-03-20
 */
export class Box {
  /**
   * Constructs a new <code>Box</code>.
   * Contains information about a box that is used in the inbound plan. The box is a container that holds multiple items.
   * @alias module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/Box
   * @class
   * @param packageId {String} Primary key to uniquely identify a Package (Box or Pallet).
   */
  constructor(packageId) {
    this.packageId = packageId;
  }

  /**
   * Constructs a <code>Box</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/Box} obj Optional instance to populate.
   * @return {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/Box} The populated <code>Box</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      switch(typeof data) {
        case 'string':
          obj = String(data);
          break;
        case 'number':
          obj = Number(data);
          break;
        case 'boolean':
          obj = Boolean(data);
          break;
      }
      obj = obj || new Box();
      if (data.hasOwnProperty('boxId'))
        obj.boxId = ApiClient.convertToType(data['boxId'], 'String');
      if (data.hasOwnProperty('contentInformationSource'))
        obj.contentInformationSource = BoxContentInformationSource.constructFromObject(data['contentInformationSource']);
      if (data.hasOwnProperty('destinationRegion'))
        obj.destinationRegion = Region.constructFromObject(data['destinationRegion']);
      if (data.hasOwnProperty('dimensions'))
        obj.dimensions = Dimensions.constructFromObject(data['dimensions']);
      if (data.hasOwnProperty('items'))
        obj.items = ApiClient.convertToType(data['items'], [Item]);
      if (data.hasOwnProperty('packageId'))
        obj.packageId = ApiClient.convertToType(data['packageId'], 'String');
      if (data.hasOwnProperty('quantity'))
        obj.quantity = ApiClient.convertToType(data['quantity'], 'Number');
      if (data.hasOwnProperty('templateName'))
        obj.templateName = ApiClient.convertToType(data['templateName'], 'String');
      if (data.hasOwnProperty('weight'))
        obj.weight = Weight.constructFromObject(data['weight']);
    }
    return obj;
  }
}

/**
 * The ID provided by Amazon that identifies a given box. This ID is comprised of the external shipment ID (which is generated after transportation has been confirmed) and the index of the box.
 * @member {String} boxId
 */
Box.prototype.boxId = undefined;

/**
 * @member {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/BoxContentInformationSource} contentInformationSource
 */
Box.prototype.contentInformationSource = undefined;

/**
 * @member {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/Region} destinationRegion
 */
Box.prototype.destinationRegion = undefined;

/**
 * @member {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/Dimensions} dimensions
 */
Box.prototype.dimensions = undefined;

/**
 * Items contained within the box.
 * @member {Array.<module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/Item>} items
 */
Box.prototype.items = undefined;

/**
 * Primary key to uniquely identify a Package (Box or Pallet).
 * @member {String} packageId
 */
Box.prototype.packageId = undefined;

/**
 * The number of containers where all other properties like weight or dimensions are identical.
 * @member {Number} quantity
 */
Box.prototype.quantity = undefined;

/**
 * Template name of the box.
 * @member {String} templateName
 */
Box.prototype.templateName = undefined;

/**
 * @member {module:fulfillmentInbound_2024-03-20/js-client.fulfillmentInbound_2024-03-20.model/Weight} weight
 */
Box.prototype.weight = undefined;


