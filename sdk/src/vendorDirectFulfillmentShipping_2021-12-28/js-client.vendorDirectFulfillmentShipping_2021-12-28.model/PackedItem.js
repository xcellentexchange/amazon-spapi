/*
 * Selling Partner API for Direct Fulfillment Shipping
 * The Selling Partner API for Direct Fulfillment Shipping provides programmatic access to a direct fulfillment vendor's shipping data.
 *
 * OpenAPI spec version: 2021-12-28
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.29
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient.js';
import {ItemQuantity} from './ItemQuantity.js';

/**
 * The PackedItem model module.
 * @module vendorDirectFulfillmentShipping_2021-12-28/js-client.vendorDirectFulfillmentShipping_2021-12-28.model/PackedItem
 * @version 2021-12-28
 */
export class PackedItem {
  /**
   * Constructs a new <code>PackedItem</code>.
   * Represents an item packed into a container for shipping.
   * @alias module:vendorDirectFulfillmentShipping_2021-12-28/js-client.vendorDirectFulfillmentShipping_2021-12-28.model/PackedItem
   * @class
   * @param itemSequenceNumber {Number} Item Sequence Number for the item. This must be the same value as sent in the order for a given item.
   * @param packedQuantity {module:vendorDirectFulfillmentShipping_2021-12-28/js-client.vendorDirectFulfillmentShipping_2021-12-28.model/ItemQuantity} Total item quantity packed in the container.
   */
  constructor(itemSequenceNumber, packedQuantity) {
    this.itemSequenceNumber = itemSequenceNumber;
    this.packedQuantity = packedQuantity;
  }

  /**
   * Constructs a <code>PackedItem</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:vendorDirectFulfillmentShipping_2021-12-28/js-client.vendorDirectFulfillmentShipping_2021-12-28.model/PackedItem} obj Optional instance to populate.
   * @return {module:vendorDirectFulfillmentShipping_2021-12-28/js-client.vendorDirectFulfillmentShipping_2021-12-28.model/PackedItem} The populated <code>PackedItem</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      switch(typeof data) {
        case 'string':
          obj = String(data);
          break;
        case 'number':
          obj = Number(data);
          break;
        case 'boolean':
          obj = Boolean(data);
          break;
      }
      obj = obj || new PackedItem();
      if (data.hasOwnProperty('itemSequenceNumber'))
        obj.itemSequenceNumber = ApiClient.convertToType(data['itemSequenceNumber'], 'Number');
      if (data.hasOwnProperty('buyerProductIdentifier'))
        obj.buyerProductIdentifier = ApiClient.convertToType(data['buyerProductIdentifier'], 'String');
      if (data.hasOwnProperty('pieceNumber'))
        obj.pieceNumber = ApiClient.convertToType(data['pieceNumber'], 'Number');
      if (data.hasOwnProperty('vendorProductIdentifier'))
        obj.vendorProductIdentifier = ApiClient.convertToType(data['vendorProductIdentifier'], 'String');
      if (data.hasOwnProperty('packedQuantity'))
        obj.packedQuantity = ItemQuantity.constructFromObject(data['packedQuantity']);
    }
    return obj;
  }
}

/**
 * Item Sequence Number for the item. This must be the same value as sent in the order for a given item.
 * @member {Number} itemSequenceNumber
 */
PackedItem.prototype.itemSequenceNumber = undefined;

/**
 * Buyer's Standard Identification Number (ASIN) of an item. Either buyerProductIdentifier or vendorProductIdentifier is required.
 * @member {String} buyerProductIdentifier
 */
PackedItem.prototype.buyerProductIdentifier = undefined;

/**
 * The piece number of the item in this container. This is required when the item is split across different containers.
 * @member {Number} pieceNumber
 */
PackedItem.prototype.pieceNumber = undefined;

/**
 * The vendor selected product identification of the item. Should be the same as was sent in the Purchase Order, like SKU Number.
 * @member {String} vendorProductIdentifier
 */
PackedItem.prototype.vendorProductIdentifier = undefined;

/**
 * Total item quantity packed in the container.
 * @member {module:vendorDirectFulfillmentShipping_2021-12-28/js-client.vendorDirectFulfillmentShipping_2021-12-28.model/ItemQuantity} packedQuantity
 */
PackedItem.prototype.packedQuantity = undefined;


