/*
 * Selling Partner API for Orders
 * Use the Orders Selling Partner API to programmatically retrieve order information. With this API, you can develop fast, flexible, and custom applications to manage order synchronization, perform order research, and create demand-based decision support tools.   _Note:_ For the JP, AU, and SG marketplaces, the Orders API supports orders from 2016 onward. For all other marketplaces, the Orders API supports orders for the last two years (orders older than this don't show up in the response).
 *
 * OpenAPI spec version: v0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.29
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient.js';
import {OpenInterval} from './OpenInterval.js';

/**
 * The BusinessHours model module.
 * @module ordersV0/js-client.ordersV0.model/BusinessHours
 * @version v0
 */
export class BusinessHours {
  /**
   * Constructs a new <code>BusinessHours</code>.
   * Business days and hours when the destination is open for deliveries.
   * @alias module:ordersV0/js-client.ordersV0.model/BusinessHours
   * @class
   */
  constructor() {
  }

  /**
   * Constructs a <code>BusinessHours</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:ordersV0/js-client.ordersV0.model/BusinessHours} obj Optional instance to populate.
   * @return {module:ordersV0/js-client.ordersV0.model/BusinessHours} The populated <code>BusinessHours</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      switch(typeof data) {
        case 'string':
          obj = String(data);
          break;
        case 'number':
          obj = Number(data);
          break;
        case 'boolean':
          obj = Boolean(data);
          break;
      }
      obj = obj || new BusinessHours();
      if (data.hasOwnProperty('DayOfWeek'))
        obj.dayOfWeek = ApiClient.convertToType(data['DayOfWeek'], 'String');
      if (data.hasOwnProperty('OpenIntervals'))
        obj.openIntervals = ApiClient.convertToType(data['OpenIntervals'], [OpenInterval]);
    }
    return obj;
  }
}

/**
 * Allowed values for the <code>dayOfWeek</code> property.
 * @enum {String}
 * @readonly
 */
BusinessHours.DayOfWeekEnum = {
  /**
   * value: "SUN"
   * @const
   */
  SUN: "SUN",

  /**
   * value: "MON"
   * @const
   */
  MON: "MON",

  /**
   * value: "TUE"
   * @const
   */
  TUE: "TUE",

  /**
   * value: "WED"
   * @const
   */
  WED: "WED",

  /**
   * value: "THU"
   * @const
   */
  THU: "THU",

  /**
   * value: "FRI"
   * @const
   */
  FRI: "FRI",

  /**
   * value: "SAT"
   * @const
   */
  SAT: "SAT"
};

/**
 * Day of the week.
 * @member {module:ordersV0/js-client.ordersV0.model/BusinessHours.DayOfWeekEnum} dayOfWeek
 */
BusinessHours.prototype.dayOfWeek = undefined;

/**
 * Time window during the day when the business is open.
 * @member {Array.<module:ordersV0/js-client.ordersV0.model/OpenInterval>} openIntervals
 */
BusinessHours.prototype.openIntervals = undefined;


